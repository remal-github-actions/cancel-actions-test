on:
  pull_request:
    types:
    - opened

permissions:
  pull-requests: 'read'
  checks: 'read'
  actions: 'write'

jobs:
  cancel-pr-actions-on-opened:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Dump github context
      env: { CONTEXT: "${{toJson(github)}}" }
      run: echo "$CONTEXT"

